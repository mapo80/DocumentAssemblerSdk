# Example 14 - Mail Merge (template-cc.docx)

This template uses content controls only, therefore no MailMerge fields should be detected.

## Run

```bash
cd DocumentAssembler/DocumentAssemblerSdk.Examples/Example14_MailMerge_TemplateCc
export PATH="$HOME/dotnet:$PATH"
dotnet run
```
