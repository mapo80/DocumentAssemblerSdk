# Example 15 - Mail Merge (template-cc-tag.docx)

Validates that the tagged content-control template does not contain MailMerge fields.

## Run

```bash
cd DocumentAssembler/DocumentAssemblerSdk.Examples/Example15_MailMerge_TemplateCcTag
export PATH="$HOME/dotnet:$PATH"
dotnet run
```
