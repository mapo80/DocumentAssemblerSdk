# Example 13 - Mail Merge (WordDocxWithALineBreak.docx)

Validates that `WordDocxWithALineBreak.docx` does not contain MailMerge fields. This ensures the
extractor handles templates without MERGEFIELD instructions correctly.

## Run

```bash
cd DocumentAssembler/DocumentAssemblerSdk.Examples/Example13_MailMerge_WordDocxWithALineBreak
export PATH="$HOME/dotnet:$PATH"
dotnet run
```
